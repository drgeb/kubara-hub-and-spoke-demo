# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually.
#
# To customize values, add additional files named values-*.yaml to this
# directory.

namespace:
  labels:
    project-name: "kubara-spoke-1"
    stage: "dev"

image:
  pullPolicy: IfNotPresent

openproject:
  https: true
  host: openproject.172.19.0.7.traefik.me
  hsts: true

  admin_user:
    password: "OpenProjectAdmin123!"
    password_reset: "false"
    name: "OpenProject Administrator"
    mail: "admin@example.com"

  cache:
    store: memcache

postgresql:
  bundled: false

  connection:
    host: "postgresql.postgresql.svc.cluster.local"
    port: 5432

  auth:
    existingSecret: openproject-postgresql
    password: "OpenProject123"
    secretKeys:
      adminPasswordKey: postgres-password
      userPasswordKey: password
    username: "openproject"
    database: "openproject"

ingress:
  enabled: true
  ingressClassName: traefik
  host: openproject.172.19.0.7.traefik.me
  path: /
  pathType: Prefix

  annotations:
    cert-manager.io/cluster-issuer: kubara-local-ca-issuer

  tls:
    enabled: true
    secretName: openproject-tls

persistence:
  enabled: true
  size: "20Gi"
  accessModes:
    - ReadWriteOnce

workers:
  default:
    replicaCount: 1
