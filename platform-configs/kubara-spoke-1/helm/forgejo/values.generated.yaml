# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually.
# To customize values, add additional files named values-*.yaml to this
# directory.

namespace:
  labels:
    project-name: "kubara-spoke-1"
    stage: "platform-engineering"

imagePullSecrets:
  - name: image-pull-secret

service:
  http:
    type: ClusterIP

ingress:
  enabled: true
  className: traefik
  annotations:
    cert-manager.io/cluster-issuer: kubara-local-ca-issuer
  hosts:
    - host: forgejo.spoke-1.kubara.test
      paths:
        - path: /
          pathType: Prefix
          port: http
  tls:
    - secretName: forgejo-tls
      hosts:
        - forgejo.spoke-1.kubara.test

gitea:
  admin:
    username: "gitea_admin"
    existingSecret: forgejo-admin
    email: "admin@local"
  env:
    - name: GITEA__database__PASSWD
      valueFrom:
        secretKeyRef:
          name: forgejo-credentials
          key: password
  config:
    server:
      DOMAIN: forgejo.spoke-1.kubara.test
      ROOT_URL: http://forgejo.spoke-1.kubara.test/
    database:
      DB_TYPE: postgres
      HOST: "postgresql.postgresql.svc.cluster.local"
      NAME: "forgejo"
      USER: "app"
      SSL_MODE: disable
