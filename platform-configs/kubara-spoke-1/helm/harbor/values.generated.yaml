# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually in any way.
# To customize values, you can add additional files named values-*.yaml to this directory.

expose:
  type: ingress
  tls:
    enabled: true
    certSource: auto
    auto:
      commonName: "harbor.spoke-1.kubara.test"
    secret:
      secretName: ""
  ingress:
    className: "traefik"
    hosts:
      core: "harbor.spoke-1.kubara.test"
    annotations:
      cert-manager.io/cluster-issuer: kubara-local-ca-issuer

externalURL: "https://harbor.spoke-1.kubara.test"
existingSecretAdminPassword: harbor-credentials
jobservice:
  initContainers:
  - name: wait-for-core
    image: busybox:1.36
    command:
    - sh
    - -c
    - |
      echo "Waiting for Harbor core to be ready..."
      until wget -qO- --no-check-certificate http://harbor-core:80/api/v2.0/ping >/dev/null 2>&1; do
        echo "  Harbor core not ready, retrying in 5s..."
        sleep 5
      done
      echo "Harbor core is ready."
database:
  type: internal
persistence:
  persistentVolumeClaim:
    database:
      size: "1Gi"
    redis:
      size: "1Gi"
    registry:
      size: "10Gi"

namespace:
  labels:
    project-name: "kubara-spoke-1"
    stage: "platform-engineering"
