# This file is rendered from a catalog template per-cluster.

ingress:
  additionalHosts:
    - my.nexus.me
  tls:
    - secretName: nexus-tls
      hosts:
        - nexus.hub.kubara.test
        - my.nexus.me